import 'package:flutter/cupertino.dart';

class AddWishViewModel with ChangeNotifier {

  /// Helper

  // 이메일 유효성 검사
  void searchWishItem(String url) {
    if (url == "") { return; }

    // 백엔드에서 url에 따른 메타 정보 가져오기!
  }
}